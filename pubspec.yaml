name: future_next
description: "Future Next - A premium career guidance app for Class 10 students in India."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.0.0

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.1
  google_fonts: ^6.2.1
  fl_chart: ^0.68.0
  confetti: ^0.7.0
  share_plus: ^9.0.0
  flutter_animate: ^4.5.0
  smooth_page_indicator: ^1.1.0
  shared_preferences: ^2.5.4
  http: ^1.2.1
  flutter_markdown: ^0.7.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
